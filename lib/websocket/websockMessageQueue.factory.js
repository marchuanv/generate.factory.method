const { WebsockMessageQueue } = require('C:\\component\\lib\\websocket\\websockMessageQueue.js');
function createWebsockMessageQueue({  }) {
    return new WebsockMessageQueue({  });
}
module.exports = { createWebsockMessageQueue };
