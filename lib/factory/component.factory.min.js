 
function createComponent({factoryContainerBindingName,packageJson,clientRequestMessageBus,clientResponseMessageBus,serverResponseMessageBus,serverRequestMessageBus}) {
    const args = {factoryContainerBindingName,packageJson,clientRequestMessageBus,clientResponseMessageBus,serverResponseMessageBus,serverRequestMessageBus};
    for(const key of Object.keys(args)) {
        ComponentFactoryConfig.ctorArgs[key] = args[key];
    };
    const container = factory.getContainer(ComponentFactoryConfig);
        
    container.ensureInstance();
    return container.references;
}
