 
function createWebSocketRequestMessage({factoryContainerBindingName,messageStatusCode,scopeId,Id,data,recipientHost,recipientPort,metadata,token,senderHost,senderPort}) {
    const args = {factoryContainerBindingName,messageStatusCode,scopeId,Id,data,recipientHost,recipientPort,metadata,token,senderHost,senderPort};
    for(const key of Object.keys(args)) {
        WebSocketRequestMessageFactoryConfig.ctorArgs[key] = args[key];
    };
    const container = factory.getContainer(WebSocketRequestMessageFactoryConfig);
        
    container.ensureInstance();
    return container.references;
}
