 
function createHttpServerMessageBus({timeout,factoryContainerBindingName,senderHost,senderPort}) {
    const args = {timeout,factoryContainerBindingName,senderHost,senderPort};
    for(const key of Object.keys(args)) {
        HttpServerMessageBusFactoryConfig.ctorArgs[key] = args[key];
    };
    const container = factory.getContainer(HttpServerMessageBusFactoryConfig);
        
    container.ensureInstance();
    return container.references;
}
