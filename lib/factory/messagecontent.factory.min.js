 
function createMessageContent({factoryContainerBindingName,data,recipientHost,recipientPort,metadata,token,senderHost,senderPort}) {
    const args = {factoryContainerBindingName,data,recipientHost,recipientPort,metadata,token,senderHost,senderPort};
    for(const key of Object.keys(args)) {
        MessageContentFactoryConfig.ctorArgs[key] = args[key];
    };
    const container = factory.getContainer(MessageContentFactoryConfig);
        
    container.ensureInstance();
    return container.references;
}
