const utils = require('utils');
const { [typename] } = require('[scriptpath]');
function [typename]Factory([args]) {
    utils.createProperty(this, 'create', () => {
        return new [typename]([args]);
    });
}
module.exports = { [typename]Factory };
