const { Factory } = require('[FactoryScriptPath]');
const container = require('[FactoryContainerFilePath]');
const factory = new Factory(container);

/**
* IsSingleton: [TypeName]FactoryContainer.singleton
* Create [TypeName]
* @param {[PrimitiveArgs]}
*/
function create[TypeName]({[PrimitiveArgsWithBindingName]}) {
    return factory.getInstance({ factoryContainerBindingName, ctorArgs: {[PrimitiveArgs]} });
}
module.exports = { create[TypeName] };
