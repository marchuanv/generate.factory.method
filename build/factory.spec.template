const { [typename]Factory } = require('[scriptpath]');
[requireScripts]
describe('when asking [typename] to create an instance', function() {
  it("it should succeed without any errors", function() {
    // Arrange
    [factoryVariableNames]
    [refArgsVariableNames]
    [nonRefArgsVariableNames]
    // Act
    const instance = new [typename]Factory([args]);
    // Assert
    expect(instance).not.toBeNull();
  });
});
