describe("when initialising a component given a userid and secret", function() {
 it("it should creste a secure messagebus", function() {
  // Arrange
  // Act
  // Assert
 });
});

describe("when sending data given a component", function() {
 it("it should instruct the messagebus to publish the data in the form of a message", function() {
  // Arrange
  // Act
  // Assert
 });
});

describe("when receiving data given a component", function() {
    it("it should instruct the messagebus to subscribe to a channel listening for messages", function() {
     // Arrange
     // Act
     // Assert
    });
   });
   